apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fx-syncstorage.fullname" . }}-test-connection"
  labels:
    {{- include "fx-syncstorage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fx-syncstorage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
