{{/*
Expand the name of the chart.
*/}}
{{- define "fx-syncstorage.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" }}
{{- end }}

{{/*
Create a default fully qualified app name.
We truncate at 63 chars because some Kubernetes name fields are limited to this (by the DNS naming spec).
If release name contains chart name it will be used as a full name.
*/}}
{{- define "fx-syncstorage.fullname" -}}
{{- if .Values.fullnameOverride }}
{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- $name := default .Chart.Name .Values.nameOverride }}
{{- if contains $name .Release.Name }}
{{- .Release.Name | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- printf "%s-%s" .Release.Name $name | trunc 63 | trimSuffix "-" }}
{{- end }}
{{- end }}
{{- end }}

{{/*
Create chart name and version as used by the chart label.
*/}}
{{- define "fx-syncstorage.chart" -}}
{{- printf "%s-%s" .Chart.Name .Chart.Version | replace "+" "_" | trunc 63 | trimSuffix "-" }}
{{- end }}

{{/*
Common labels
*/}}
{{- define "fx-syncstorage.labels" -}}
helm.sh/chart: {{ include "fx-syncstorage.chart" . }}
{{ include "fx-syncstorage.selectorLabels" . }}
{{- if .Chart.AppVersion }}
app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
{{- end }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- end }}

{{/*
Selector labels
*/}}
{{- define "fx-syncstorage.selectorLabels" -}}
app.kubernetes.io/name: {{ include "fx-syncstorage.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}

{{/*
Create the name of the service account to use
*/}}
{{- define "fx-syncstorage.serviceAccountName" -}}
{{- default (include "fx-syncstorage.fullname" .) .Values.serviceAccount.name }}
{{- end }}

{{/*
Helper boolean for if handling is needed for automatically generated mysql subchart credentials
*/}}
{{- define "fx-syncstorage.autogenerateDbCredentials" -}}
{{- if or (not .Values.syncstorage.syncserver.databaseUrl) (not .Values.syncstorage.tokenserver.databaseUrl) }}
true
{{- else }}
{{- end }}
{{- end }}

{{- define "fx-syncstorage.autogenerateTokenDb" -}}
{{ $autogeneratedSecret := (eq .Values.tokenserverdb.auth.existingSecret "autogenerated-fx-tokenserverdb") }}
{{- if and (include "fx-syncstorage.autogenerateDbCredentials" .) $autogeneratedSecret }}
true
{{- else }}
{{- end }}
{{- end }}

{{- define "fx-syncstorage.autogenerateSyncDb" -}}
{{ $autogeneratedSecret := (eq .Values.syncserverdb.auth.existingSecret "autogenerated-fx-syncserverdb") }}
{{- if and (include "fx-syncstorage.autogenerateDbCredentials" .) $autogeneratedSecret }}
true
{{- else }}
{{- end }}
{{- end }}

{{- define "fx-syncstorage.customDbUrls" }}
{{- if or .Values.syncstorage.syncserver.databaseUrl .Values.syncstorage.tokenserver.databaseUrl }}
true
{{- else }}
{{- end }}
{{- end }}

{{/*
Test required parameters
*/}}
{{- if not .Values.syncstorage.domain }}
{{- fail "syncstorage.domain must be set!" }}
{{- end }}
